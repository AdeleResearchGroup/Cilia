To compile execute
$mvn clean install

In order to execute you must go to the gateway folder and execute the "cilia" script.

To see the application go to localhost:8080/suiviConso


This is a simple application performing a split/aggregate pattern.
The service provided the information are simulated and they return random generated values.